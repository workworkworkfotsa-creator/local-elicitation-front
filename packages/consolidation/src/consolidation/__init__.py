"""consolidation — block 2 (STUB, hors scope).

Will collect many ``expression_de_besoin``, deduplicate, prioritise and route them toward
``generation``. This is the formerly ambiguous "EB" node. Reads only ``contract``.

Status: STUB — no logic until the front oracles are green. Glossary: docs/dictionnaire-metier.md.
"""
